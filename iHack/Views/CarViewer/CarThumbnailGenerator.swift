import Foundation
import AppKit

// MARK: - Thumbnail Generation

class CarThumbnailGenerator {
    
    static func createPlaceholderThumbnail(for assetName: String) -> NSImage? {
        // Return nil so we show proper "no preview" symbols instead of fake thumbnails
        return nil
    }
}
